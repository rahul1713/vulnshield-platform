# DO NOT commit real secrets. Copy to secrets.yaml and apply, or use SealedSecrets / External Secrets.
# cp k8s/secrets.example.yaml k8s/secrets.yaml
# kubectl apply -f k8s/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: vulnshield-secrets
  namespace: vulnshield
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_WITH_OPENSSL_RAND_HEX_24"
  RABBITMQ_PASSWORD: "REPLACE_WITH_OPENSSL_RAND_HEX_24"
  REDIS_PASSWORD: "REPLACE_WITH_OPENSSL_RAND_HEX_24"
  JWT_SECRET: "REPLACE_WITH_OPENSSL_RAND_HEX_32"
  MINIO_ACCESS_KEY: "REPLACE_WITH_RANDOM_ACCESS_KEY"
  MINIO_SECRET_KEY: "REPLACE_WITH_OPENSSL_RAND_HEX_24"
  INIT_ADMIN_PASSWORD: "REPLACE_WITH_STRONG_ADMIN_PASSWORD"
